UPDATE QC_MASTER.KEYLOOKUP
   SET CURRENTVALUE = (SELECT MAX(PINCODEMASTERID)
                         FROM QC_MASTER.QM_PINCODEMASTER) 
                         WHERE 
                      TABLENAME = 'QM_PINCODEMASTER'
